
Missing classes:

If a table or view does not have a primary key defined and no valid primary key could be inferred,
this table or view will be been excluded when creating ADO.NET Entity Data Model (code first from db). 
To use the entity, you will need to review your schema, add the correct keys.